{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.0",
  "moduleType": [
    "amd"
  ],
  "private": true,
  "ignore": [
    "**/.*",
    "node_modules",
    "bower_components",
    "test",
    "tests"
  ],
  "devDependencies": {
    "jquery": "components/jquery#~2.1.0",
    "requirejs": "~2.1.14"<% if (bootstrap) { %>,
    "bootstrap": "~3.2.0"<% } if (backbone) { %>,
    "backbone": "~1.1.2"<% } %>
  }
}

