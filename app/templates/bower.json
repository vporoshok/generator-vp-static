{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.0",
  "moduleType": [
    "amd"
  ],
  "private": true,
  "ignore": [
    "**/.*",
    "node_modules",
    "bower_components",
    "test",
    "tests"
  ],
  "devDependencies": {
    "jquery": "components/jquery#*",
    "requirejs": "*",
    "normalize-css": "*"<% if (backbone) { %>,
    "backbone": "*"<% } if (bootstrap) { %>,
    "bootstrap": "*"<% } if (font_awesome) { %>,
    "components-font-awesome": "*"<% } if (lightbox) { %>,
    "lightbox2": "*"<% } if (lodash) { %>,
    "lodash": "*"<% } %>
  }
}

